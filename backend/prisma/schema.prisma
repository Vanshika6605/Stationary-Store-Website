generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // The url line has been removed
}

enum Role {
  ADMIN
  NORMAL
  STORE_OWNER
}

// ... rest of your models (User, Store, Rating) stay exactly the same